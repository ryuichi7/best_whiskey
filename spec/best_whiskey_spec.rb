require 'spec_helper'

describe BestWhiskey do
  it 'has a version number' do
    expect(BestWhiskey::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
